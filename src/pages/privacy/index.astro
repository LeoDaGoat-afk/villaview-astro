---
import Layout from '../../layouts/Layout.astro';
import PolicyContent from '../../components/PolicyContent.astro';
import { getLangFromPath, useT } from '../../i18n/utils';
import { privacy, pickPolicy } from '../../lib/policy-content';

const lang = getLangFromPath(Astro.url.pathname);
const t = useT(lang);
const body = pickPolicy(privacy, lang);
---
<Layout title={t('page.title.privacy')}>
  <PolicyContent title={t('policy.privacy.title')} body={body} />
</Layout>
