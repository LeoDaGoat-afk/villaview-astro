# Cloudflare Pages redirects for VILLA VIEW
#
# 2026-08-12: 存在しないURLがトップページを HTTP 200 で返していた（ソフト404）。
# Google はこれをエラーとして扱い、存在しないURLが無限にインデックス候補になる。
# Cloudflare Pages は静的アセットを先に解決するため、実在するページはこの
# ルールにマッチしない。どのファイルにも該当しなかったときだけ 404 を返す。
/*    /404.html    404
